apiVersion: apps/v1
kind: Deployment
metadata:
  name: common-service
#  namespace: goormone-msa
  labels:
    app: common-service
    component: msa
spec:
  replicas: 2
  selector:
    matchLabels:
      app: common-service
  template:
    metadata:
      labels:
        app: common-service
        component: msa
    spec:
      # eks 에서만
      #      nodeSelector:
      #        node-type : app
      containers:
        - name: common-service
#          image: 490913547024.dkr.ecr.ap-northeast-2.amazonaws.com/common-service:latest
          image: bleuxsy/common-service:latest
          ports:
            - containerPort: 8080
              name: http
#          envFrom:
#            - configMapRef:
#                name: msa-config
#            - secretRef:
#                name: msa-secret
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "dev"
            - name: JAVA_OPTS
              value: "-Dspring.profiles.active=dev"
          resources:
            requests:
              cpu: 500m
              memory: 512Mi
            limits:
              cpu: 1000m
              memory: 1Gi
#          livenessProbe:
#            httpGet:
#              path: /actuator/health/liveness
#              port: 8080
#            initialDelaySeconds: 60
#            periodSeconds: 30
#          readinessProbe:
#            httpGet:
#              path: /actuator/health/readiness
#              port: 8080
#            initialDelaySeconds: 30
#            periodSeconds: 10
