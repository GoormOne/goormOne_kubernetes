apiVersion: v1
kind: Service
metadata:
  name: order-service
  #  namespace: goormone-msa
  labels:
    app: order-service
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: order-service
